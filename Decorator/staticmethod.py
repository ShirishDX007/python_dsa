class Unit_converter:

    @staticmethod
    def length_converter(number):
        """Converts length from meters to milimeter"""
        return number * 1000
    @staticmethod
    def area_converter(area):
        return 
convert = Unit_converter.length_converter(2)
print(convert)